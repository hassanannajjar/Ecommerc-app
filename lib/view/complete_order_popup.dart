import 'package:flutter/material.dart';

class CompleteOrderPopup extends StatelessWidget {
  const CompleteOrderPopup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CompleteOrderPopup Screen'),
    );
  }
}
